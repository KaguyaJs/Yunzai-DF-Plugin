/** 通用Redis Key前缀 */
export const commonRedisKey = 'DF'

/** 联系主人Key前缀 */
export const sendMasterRedisKey = `${commonRedisKey}:contact`

/** CodeUpdate Redis Key前缀 */
export const CodeUpdateRedisKey = `${commonRedisKey}:CodeUpdate`
